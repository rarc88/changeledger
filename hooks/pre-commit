#!/bin/sh
# Spec Ledger quality gate. lint-staged autoformats staged files with Biome
# (it stashes unstaged changes first, so partial commits stay intact), then the
# full gate runs: lint + test + sl check. Blocks the commit on failure.
# Enable in this repo with: git config core.hooksPath hooks
npx lint-staged || exit 1
exec pnpm verify
